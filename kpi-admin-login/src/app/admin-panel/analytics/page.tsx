export default function AnalyticsPanelPlaceholderPage() {
  return (
    <main className="mx-auto w-full max-w-3xl px-6 py-10">
      <h1 className="mb-2 text-3xl font-semibold text-(--text-primary)">Analytics Prototype</h1>
      <p className="text-sm text-(--text-muted)">
        `admin-panel/analytics` does not yet contain a page component. Add an exported page
        file and mount it here.
      </p>
    </main>
  );
}
